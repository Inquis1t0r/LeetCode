/**
 * @param {string[]} words
 * @param {character} x
 * @return {number[]}
 */
var findWordsContaining = function(words, x) {
    const indices = new Array();
    for (var i = 0; i < words.length; i++) {
         console.log(words[i]); 
         }

};
